spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db:5432/coffe-world}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:admin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:changeme}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# CORS — em dev aceita localhost
app.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:4200,http://localhost:80}

# =============================================
# ATENCAO: este arquivo eh o profile DEFAULT (dev).
# Os valores abaixo sao APENAS PARA DESENVOLVIMENTO LOCAL.
# Em producao o profile 'prod' EXIGE variaveis de ambiente reais.
# =============================================

# JWT (dev) — chave obviamente fake, prod usa JWT_SECRET do Railway
jwt.secret=${JWT_SECRET:ZEVWLU9OTFktTk9ULUZPUi1QUk9ELU5FVkVSLVVTRS1USElTLVNFQ1JFVC1PTkxZLUxPQ0FM}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Seed de usuarios (dev) — credenciais inofensivas, prod exige env vars
app.admin.email=${ADMIN_EMAIL:admin@local.dev}
app.admin.password=${ADMIN_PASSWORD:devadmin}
app.admin.name=${ADMIN_NAME:Admin Local}
app.cozinha.email=${COZINHA_EMAIL:cozinha@local.dev}
app.cozinha.password=${COZINHA_PASSWORD:devcozinha}
